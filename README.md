This repository contains the Markdown sources for the webpage at <https://gbgmc.github.io/>

## Local build

Install Ruby and then run `gem install bundler`.

In the git working copy run
```
bundler install
bundle exec jekyll build
```

A live preview can be done via
```
bundle exec jekyll server
```
